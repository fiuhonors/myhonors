(function (window) {
    'use strict';

    window.SETTINGS = {
        STORAGE: {
            FIREBASE: {
                BASE_URL: ""
            }
        },
        COMPILE_PROVIDER: {
            DEBUG_INFO_ENABLED: true,
        },
        HTTP_PROVIDER: {
            USE_APPLY_ASYNC: true
        }
    };

}(window));