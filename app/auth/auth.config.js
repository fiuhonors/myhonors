(function (window) {
	'use strict';
	
	var angular = window.angular;
	
	angular
		.module('myhonorsApp.auth')
		.config(config);
	
	config.$inject = [];
	
	function config(OAuthProvider) {
		
	}
	
}(window));